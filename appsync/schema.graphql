type Tender {
  id: ID!
  procurementId: String
  owner: String
  title: String
  description: String
}

type HivebriteUser{
  id: ID
  email: String
  name: String
}

type HivebiretUserConnection{
  total: Int
  users: [HivebriteUser]
}

type Query {
  getTender(id: ID): Tender
  hivebriteUsers(pageStart: Int!, perPage: Int!, pageMax: Int ): HivebiretUserConnection
}
